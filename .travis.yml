language: cpp

# safelist
branches:
  only:
  - master
  - dev

compiler:
  - gcc

cache: ccache

os:
  - linux

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libevent-dev
      - gcc-4.8
      - g++-4.8
      - libboost-all-dev



before_install:


install:
  - if [[ "$TRAVIS_OS_NAME" != "osx" && "$CXX" = "g++" ]]; then export CXX="g++-4.8" CC="gcc-4.8"; fi

script: 

git clone https://github.com/Cylix/cpp_redis.git&& cd cpp_redis&&mkdir build&&cd build&&cmake ..&&make&&make install&&cd ../..&&ls&&mkdir build && cd build && cmake .. && make && ./bin/translib_tests
